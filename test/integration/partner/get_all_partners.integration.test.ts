import { describe, it, expect } from "vitest"

describe("[INTEGRATION] GetAllPartner", () =>{
    it("should get all partners via API (exemple)", async () =>{
        expect(true).toBe(true)
    })
})