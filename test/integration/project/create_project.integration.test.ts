import { describe, it, expect } from "vitest"

describe("[INTEGRATION] CreateProject", () =>{
    it("should create a project via API (exemple)", async () =>{
        expect(true).toBe(true)
    })
})