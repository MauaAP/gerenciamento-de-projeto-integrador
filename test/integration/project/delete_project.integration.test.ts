import { describe, it, expect } from "vitest"

describe("[INTEGRATION] DeleteProject", () =>{
    it("should delete a project via API (exemple)", async () =>{
        expect(true).toBe(true)
    })
})